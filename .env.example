# Required — session JWT signing secret (generate with: openssl rand -base64 32)
SESSION_SECRET=

# Optional — without these, OTP codes are logged to the server console instead of sent via SMS
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
